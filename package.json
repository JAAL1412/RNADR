{
  "name": "type2",
  "version": "1.0.0",
  "description": "Scan",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"uvicorn api2:app --host 0.0.0.0 --port 8000\" \"node ap1.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Liscencia para innovar mas alla de la pantalla",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "cap": "^0.2.1",
    "concurrently": "^9.1.2"
  }
}
